<div class="container">
	<div class="page-header" id="banner">
		<div class="row">
			<div class="col-xs-12">
				<h1 id="nav-tabs"><%=t(:trainings)%></h1>
			</div>
		</div>
		<div class="row">
			<div class="col-xs-12">
				<ul class="breadcrumb">
					<li><%=link_to t(:home), root_path%></li>
					<li class="active"><%=t(:trainings)%></li>
				</ul>
			</div>
		</div>
		<div class="row">
			<div class="col-xs-12">
				<%=render :partial => "tools_partial"%>
			</div>
		</div>
	</div>
</div>

<div class="container">
	<div class="row">
		<div class="col-xs-12">
			<table class="table table-striped">
				<thead>
					<tr>
						<th>ID</th>
						<th><%=t(:name)%></th>
						<th><%=t(:description)%></th>
						<th style="text-align: center; width: 170px;"><%=t(:actions)%></th>
					</tr>
				</thead>
				<tbody>
					<%if @trainings.blank?%>
					<tr>
						<td colspan="4"> No hay datos para mostrar.</td>
					</tr>
					<%else%>
						<%@trainings.each do |t|%>
						<tr>
							<td><%=t.id%></td>
							<td><%=t.name%></td>
							<td><%=t.description%></td>
							<td style="text-align: center;">
								<div class="btn-group">
									<%=link_to fa_icon("eye fa-4")+" "+t(:view), training_path(t.id), {:class => "btn btn-default"}%>
									<%=link_to fa_icon("edit fa-4")+" "+t(:edit), edit_training_path(t.id), {:class => "btn btn-default"}%>
								</div>
							</td>
						</tr>
						<%end%>
					<%end%>
				</tbody>
			</table>
		</div>
	</div>
</div>
